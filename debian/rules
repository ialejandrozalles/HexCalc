#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	install -D -m 0755 hexcalc.py debian/hexcalc/usr/bin/hexcalc

override_dh_auto_build:
	# No build needed for Python script

override_dh_auto_test:
	# Skip tests for now

override_dh_auto_clean:
	# No cleanup needed
